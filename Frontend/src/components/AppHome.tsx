import React from "react";
import {Container, CssBaseline, Typography} from "@mui/joy";

export const AppHome = () => {
    return (
        <React.Fragment>
            <CssBaseline />
            <Container maxWidth="xl">
                <Typography component="h1" gutterBottom>
                </Typography>
            </Container>

        </React.Fragment>
    );
};